[33mcommit f239be894d2dafd1b278d6882b10a6809975aabf[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mSPViT[m[33m, [m[1;31morigin/SPViT[m[33m)[m
Author: why-sobi <sobisoban05@gmail.com>
Date:   Thu Apr 23 16:16:55 2026 +0500

    Added Centering of object

[1mdiff --git a/Simulation World/controllers/spvit_detector/spvit_detector.py b/Simulation World/controllers/spvit_detector/spvit_detector.py[m
[1mindex 61678f9..7ea4ca7 100644[m
[1m--- a/Simulation World/controllers/spvit_detector/spvit_detector.py[m	
[1m+++ b/Simulation World/controllers/spvit_detector/spvit_detector.py[m	
[36m@@ -47,13 +47,15 @@[m [mprint("[INIT] CLIP loaded successfully.")[m
 # Settings[m
 # ---------------------------[m
 SEARCH_PROMPT        = "a round ball with black and white patches"[m
[31m-CENTER_TOL           = 0.05   # ±15% of frame width → considered centred[m
[32m+[m[32mCENTER_TOL           = 0.03 # ±15% of frame width → considered centred[m
[32m+[m[32mROTATE_ONLY_TOL      = 0.40 # If error is > 40%, stop moving forward and just spin[m
 CONFIDENCE_THRESHOLD = 20.0[m
 EMA_ALPHA            = 0.4[m
[32m+[m[32mEMP_CENTER           = 3.02 # empirically determined center_x value when object is perfectly centered (for error normalization)[m[41m  [m
 [m
 # Drive constants[m
[31m-BASE_SPEED  = MAX_SPEED * 0.40   # forward creep while centering[m
[31m-TURN_GAIN   = MAX_SPEED * 0.50   # steering authority[m
[32m+[m[32mBASE_SPEED  = MAX_SPEED * 0.35   # forward creep while centering[m
[32m+[m[32mTURN_GAIN   = MAX_SPEED * 0.80   # steering authority[m
 SEARCH_SPEED = MAX_SPEED * 0.30  # spin speed when object not found[m
 [m
 # Frame-skip: run CLIP every N steps[m
[36m@@ -64,7 +66,7 @@[m [mINFERENCE_EVERY_N = 3[m
 # ---------------------------[m
 GRID_SIZE = 7[m
 _xs = torch.arange(GRID_SIZE).unsqueeze(0).repeat(GRID_SIZE, 1).flatten().float()[m
[31m-[m
[32m+[m[32mprint(f"[INIT] _XS grid pre-computed: {_xs.shape}  values={_xs.numpy()} ...")[m
 [m
 def get_frame_as_pil(cam):[m
     raw = cam.getImage()[m
[36m@@ -78,28 +80,33 @@[m [mdef get_frame_as_pil(cam):[m
 [m
 def compute_speeds(error):[m
     """[m
[31m-    Given a normalised lateral error (−1 … +1), return motor speeds[m
[31m-    that keep the detected object centred in the frame.[m
[31m-[m
[31m-    States[m
[31m-    ------[m
[31m-    CENTRED  : error within tolerance → drive straight[m
[31m-    LEFT     : object is left of centre → turn left[m
[31m-    RIGHT    : object is right of centre → turn right[m
[32m+[m[32m    Improved control logic:[m[41m [m
[32m+[m[32m    1. If very far off, spin in place.[m
[32m+[m[32m    2. If somewhat off, arc toward the object.[m
[32m+[m[32m    3. If centered, drive straight.[m
     """[m
[31m-    if abs(error) < CENTER_TOL:[m
[31m-        # Already centred — drive straight[m
[31m-        return BASE_SPEED, BASE_SPEED, "CENTRED"[m
[31m-[m
[31m-    # Reduce forward speed proportionally to how far off-centre the object is,[m
[31m-    # and add a turn delta to steer toward it.[m
[31m-    forward_speed = BASE_SPEED * (1.0 - 0.4 * abs(error))[m
[31m-    turn_delta    = TURN_GAIN  * error[m
[31m-[m
[31m-    left_speed  = max(-MAX_SPEED, min(MAX_SPEED, forward_speed + turn_delta))[m
[31m-    right_speed = max(-MAX_SPEED, min(MAX_SPEED, forward_speed - turn_delta))[m
[31m-[m
[31m-    direction = "RIGHT" if error > 0 else "LEFT"[m
[32m+[m[32m    abs_err = abs(error)[m
[32m+[m[41m    [m
[32m+[m[32m    # CASE 1: Centered (Drive straight)[m
[32m+[m[32m    if abs_err < CENTER_TOL:[m
[32m+[m[32m        return BASE_SPEED, BASE_SPEED, "STRICT_CENTER"[m
[32m+[m
[32m+[m[32m    # CASE 2: Far off-center (Rotate in place)[m
[32m+[m[32m    # This prevents the robot from 'orbiting' the object[m
[32m+[m[32m    if abs_err > ROTATE_ONLY_TOL:[m
[32m+[m[32m        # High gain rotation, zero forward velocity[m
[32m+[m[32m        turn_speed = TURN_GAIN * (error / abs_err) # Keeps direction sign[m
[32m+[m[32m        return turn_speed, -turn_speed, "STATIONARY_TURN"[m
[32m+[m
[32m+[m[32m    # CASE 3: Moderate error (Arcing/Proportional steering)[m
[32m+[m[32m    # We use a non-linear scaling so it slows down forward speed as it turns[m
[32m+[m[32m    forward_speed = BASE_SPEED * (1.0 - (abs_err / ROTATE_ONLY_TOL))[m
[32m+[m[32m    turn_delta    = TURN_GAIN * error[m
[32m+[m[41m    [m
[32m+[m[32m    left_speed  = max(-MAX_SPEED, min(MAX_SPEED, forward_speed - turn_delta))[m
[32m+[m[32m    right_speed = max(-MAX_SPEED, min(MAX_SPEED, forward_speed + turn_delta))[m
[32m+[m
[32m+[m[32m    direction = "ARC_RIGHT" if error < 0 else "ARC_LEFT"[m
     return left_speed, right_speed, direction[m
 [m
 [m
[36m@@ -148,14 +155,15 @@[m [mwhile robot.step(timestep) != -1:[m
             text_emb  = text_emb / text_emb.norm(dim=-1, keepdim=True)[m
 [m
             heatmap  = torch.matmul(projected[0], text_emb.t()).squeeze()[m
[31m-            weights  = torch.softmax(heatmap, dim=0)[m
[32m+[m[32m            weights  = torch.softmax(heatmap / 0.1, dim=0)[m
 [m
             center_x  = (weights * _xs).sum().item()[m
[31m-            center    = (GRID_SIZE - 1) / 2[m
[31m-            raw_error = (center_x - center) / center        # −1 (left) … +1 (right)[m
[32m+[m[32m            # center    = (GRID_SIZE - 1) / 2[m
[32m+[m[32m            raw_error = (center_x - EMP_CENTER) / EMP_CENTER        # −1 (left) … +1 (right)[m
 [m
[32m+[m[32m            # print(f"DEBUG: center_x={center_x:.2f} raw_error={raw_error}")[m
             # EMA smoothing to reduce frame-to-frame jitter[m
[31m-            last_error = EMA_ALPHA * raw_error + (1.0 - EMA_ALPHA) * last_error[m
[32m+[m[32m            last_error = raw_error[m
 [m
     # ---------------------------[m
     # Motor control[m
[36m@@ -169,8 +177,8 @@[m [mwhile robot.step(timestep) != -1:[m
     else:[m
         # Object not detected → spin in place to search[m
         last_error  = 0.0          # clear stale error before re-acquisition[m
[31m-        left_speed  =  SEARCH_SPEED[m
[31m-        right_speed = -SEARCH_SPEED[m
[32m+[m[32m        left_speed  = -SEARCH_SPEED[m
[32m+[m[32m        right_speed = SEARCH_SPEED[m
 [m
         if run_inference:[m
             print(f"[SEARCHING] score={last_logits:.2f}")[m
